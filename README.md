This is an example of encoding / decoding a message with the use of an encryption-key and bit-manipulation by inverting bits and running XOR ops to encrypt a message.

The application also prints out the Raw Bytes before encryption, encrypted bytes and finally the decoded bytes.

in the end a comparison is checked to see if the output message matches the input message.
